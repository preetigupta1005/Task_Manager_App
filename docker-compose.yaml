services:
    db:
        image: 'postgres:13'
        ports:
            - '5433:5432'
        volumes:
            - ./pgdata:/var/lib/postgresql/data
        environment:
                - POSTGRES_USER=preeti
                - POSTGRES_PASSWORD=1234
                - POSTGRES_DB=tododb